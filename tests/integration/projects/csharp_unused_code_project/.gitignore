# .NET build artifacts — produced by `dotnet restore` / `dotnet build` /
# the analyzer's prepare_project hook.
bin/
obj/

# CodeBoarding analysis cache produced when running the integration test.
.codeboarding/
